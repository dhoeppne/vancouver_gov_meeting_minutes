You are generating analyst-grade meeting reports for Vancouver City Council. The scraped data lives under the directory __DATA_DIR__ (the wrapper substitutes the real absolute path). The git repository — your current working directory — holds only the scripts. Data body directory: __DATA_DIR__/vancouver_city_council.

A nightly scraper has already downloaded every meeting document and referenced bylaw and extracted their text. Do NOT fetch anything from the web, and do NOT run any git commands — a wrapper script handles email and any git. Synthesize only from files already on disk.

Steps:
1. Read __DATA_DIR__/vancouver_city_council/manifest.json. Build the work list: every meeting key (format YYYY-MM-DD_type) where the file __DATA_DIR__/vancouver_city_council/meetings/<key>/minutes.txt exists AND __DATA_DIR__/vancouver_city_council/reports/<key>.pdf does NOT exist. Never modify or rewrite an existing report. If a report's .md exists but its .pdf is missing, only re-run the render step (step 4) for it.
2. If the work list is empty, print "No unreported Vancouver City Council meetings." and stop.
3. Process at most 5 meetings, oldest first. For each meeting read: minutes.txt, agenda.txt if present, every attachment .txt, and the .txt of every bylaw listed in that meeting's manifest entry (the paths in the manifest are relative to __DATA_DIR__/vancouver_city_council/). If the manifest marks text_quality as poor_or_scanned, still write the report but state the limitation in the TL;DR.
4. Write __DATA_DIR__/vancouver_city_council/reports/<key>.md with exactly this structure:
   - # Vancouver City Council — <Type Label>, <Month D, YYYY>   (Type Label from the manifest entry)
   - ## TL;DR  (max 5 bullets)
   - ## Key Decisions & Votes  (each motion: outcome, mover if recorded, vote split if recorded)
   - ## Bylaws & Policies Enacted or Discussed  (for each bylaw: what it actually changes, grounded in the bylaw text you read)
   - ## Money & Budget Items  (dollar figures, funding sources)
   - ## Contentious Items & Public Delegations  (who spoke, on what, friction points)
   - ## What to Watch Next  (deferred items, upcoming hearings, follow-ups)
   Ground every claim in the documents; never invent votes, names, or figures — write "not recorded in minutes" when something is absent. Then render the PDF (the render script is in your working directory):
   .venv/bin/python scripts/md_to_pdf.py __DATA_DIR__/vancouver_city_council/reports/<key>.md __DATA_DIR__/vancouver_city_council/reports/<key>.pdf
5. Print a one-line summary: how many reports you created and how many unreported meetings remain.

Guardrails: never touch files outside __DATA_DIR__/vancouver_city_council/reports/; never delete or edit an existing report; never run git or any network command; if one meeting fails, skip it and continue with the rest.
